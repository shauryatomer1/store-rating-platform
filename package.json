{
    "name": "store-rating-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd backend && npm install",
        "build": "cd backend && npm install",
        "start": "cd backend && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}